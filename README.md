## Installation

    $ bundle install
    for install all gem dependencies 

## Running the tests

    $ bundle exec rspec
    will run all spec files

## files:

    page objects structure:
      under lib/page_objects/
    specs (tests)
      under spec/
